version: "2"
run:
  timeout: 10m
  concurrency: 8
linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - asciicheck
    - dogsled
    - goheader
    - unused
    - gosec
    - misspell
    - rowserrcheck
    - sqlclosecheck
    - revive
    - funlen
    - unparam
    - errorlint
    - bodyclose
    - gocritic
    - nilerr
    - ireturn
    - importas
    - wsl
    - copyloopvar
    - nilerr
    - makezero
    - reassign
  settings:
    funlen:
      lines: -1
      statements: -1
    gosec:
      excludes:
        - G601
        - G304
        - G101
    staticcheck:
      checks:
        - all
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gofmt
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
